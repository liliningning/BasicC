#include <stdio.h>
#include <string.h>

#define BUFFER_SIZE 40

/*数组
  1. 一断连续的存储空间
  2. 它里面存放的数据内型是相同的

     数组的越界访问

*/
int main()
{


#if 0    
    int array[BUFFER_SIZE] = {0};
// 判断索引的有效性： 索引必须是合理的范围 range (0~BUFFER_SIZE-1)
    printf("array[-1]%d\n", array[-1]);
    printf("array[10]%d\n", array[10]);
    
#endif    
    
#if 0
   
    //静态数组  
    int desk [BUFFER_SIZE] = { 0 };
    
    
    
    
    // 动态数组






#endif    


#if 1




    
#endif    
    return 0;
}
